name: stella_chatbot
description: A parenting chatbot app powered by GPT-4 mini.

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  provider: ^6.0.0
  shared_preferences: ^2.0.6
  mongo_dart: ^0.7.3
  flutter_chat_ui: ^1.5.8

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/